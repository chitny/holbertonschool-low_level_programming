#include <stdio.h>

/**
 * main - prints from 0 to 9 in base 10
 * Return: if everything is ok, 0
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
